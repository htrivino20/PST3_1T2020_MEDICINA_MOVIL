<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medicinamovil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PrincipalEnfermeroActivity"></activity>
        <activity android:name=".ObjetosNat.PrincipalEnfermeroActivity"></activity>
        <activity android:name=".AlertActivity" />
        <activity android:name=".SolicitudActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".ObjetosNat.InfoActivity" />
        <activity android:name=".Fragments.InfoActivity" />
        <activity android:name=".PrincipalPacienteActivity" />
        <activity android:name=".IniciarSesionActivity" />
        <activity android:name=".GenerarContrasenaActivity" />
        <activity android:name=".InicioActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>